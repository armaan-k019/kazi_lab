import { StoryPage } from "@/components/story/story-page";

// The front door: the lab telling its own story as you scroll, grounded in
// live state. The working dashboard lives at /lab (linked both ways).
export default function Home() {
  return <StoryPage />;
}
